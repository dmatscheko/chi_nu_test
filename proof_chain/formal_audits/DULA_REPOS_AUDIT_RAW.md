# DULA_REPOS_AUDIT_RAW — file-by-file triage of three further DULA2025 repos

Audit date: 2026-07-06. Auditor: Claude (file-reading audit; **none of the three repos has a lakefile/toolchain**, so nothing is machine-checked in situ).
Scope: `dula/DULA-THEOREM---LEAN` (9 .lean), `dula/Riemann-Hypothesis-Proof` (4 Lean sources incl. the extensionless `RH_LEAN4_Proof_Aristotle`, plus README/PDF/Python context), `dula/Geometric-Propulsion-Hardware` (README, Diagnostics.md, 10 PDFs, 1 .sage, 1 .stl).
Verdict tiers as in PIE_AUDIT_RAW: **REAL-MATH** / **DECORATIVE** / **NUMEROLOGY-CHECK** / **BROKEN** (live sorry or content-bearing custom axiom) / **UNBUILDABLE**. Counts below are **live** (comment-stripped; e.g. "Fully sorry-free" doc-comments produce raw grep hits).

Companion audit: `PIE_AUDIT_RAW.md` (repo now at `dula/prime-inertia-engine`). Git provenance: all commits authored "DULA"; DULA_Theorem drafts Aug–Oct 2025, RH repo Nov 2025–Feb 2026, DULA-THEOREM 2026-02-26/27, hardware repo 2026-03-31 (two days after the PIE "Certificate of Formal Verification" PDF dated 2026-03-29). Named co-authors inside the artifacts: Grok/GROK 4, Gemini 2.5 Pro/"Gemini-3", Aristotle (Harmonic), Claude.

---

## 0. Executive orientation

1. **Same two-voice disease as PIE, worse ratio.** The honest voice is again the machine: Aristotle's in-file summaries state plainly that Navier–Stokes was "proved under simplified definitions" (`smooth := True`), that the user's L-function symmetry claim "is false as stated" (and proves the negation), and that BSD scaffolding uses "dummy" constant-zero definitions. The human/marketing voice claims "FULLY FIXED & ARISTOTLE-VERIFIED", "100% ready for GitHub", "Fully sorry-free … completely machine-checkable", "All core theorems fully proved" — on files that cannot compile (nonexistent lemmas, Lean-3 syntax, English prose inside an `axiom`, a literal `...` inside a theorem statement).
2. **Millennium-problem pattern.** Every headline "conjecture" file is one of: (i) the honest open statement carrying a `sorry` (BSD full, Collatz), (ii) a conclusion-shaped axiom/hypothesis unpacked in 4 lines (RH ×3 files, BSD rank ≤ 1), or (iii) a `True`-stub tautology (Navier–Stokes, where no PDE appears in the file at all). None states-and-proves any actual open conjecture.
3. **The flagship "BSD rank 5 verified" is fiction three ways.** In Lean it is `analyticRank := 5` (a bare class field, with `L := fun _ => 0` as "Dummy definition") followed by `theorem dula_bsd_990h3 : … = 5 := rfl`, or `exact (by decide : 5 = 5)`. In HTML it is a three.js animation of five rainbow rings titled "Birch & Swinnerton-Dyer Conjecture — Visually Destroyed". And arithmetically, a rank-5 curve at conductor 990 contradicts the known tables (smallest known rank-5 conductor ≈ 1.9×10⁷); the user's own `ofWeierstrass 1 1 0 (-1) (-79) 289` call even passes six a-invariants where Weierstrass form has five.
4. **Genuinely real content is small and classical:** the Aristotle-proven mod-6 grading family (n ≡ (−1)^{m₅(n)} mod 6, φ₆ monoid hom, congruence recovery — cleanest in `dula_goldbach_conjecture.lean`), the height-pairing polarization block (bilinearity/symmetry/positive-definiteness from an axiomatized parallelogram law in `GL(1)–GL(2) and BSD.lean`), and Aristotle's refutation lemmas (Mellin-integral divergence at s = −1 disproving the user's functional-equation claim). All standard mathematics.
5. **The hardware repo is an LLM-guided Biefeld–Brown lifter build** (needle emitter + mesh collector + 20–50 kV ignition-coil/flyback pulsed at 28.87 Hz) wrapped in "Ricci Flow Surgery of the arithmetic vacuum" language, with explicit propellantless-thrust claims and a fictional safety system. **This is the only artifact in the DULA corpus that could physically hurt someone.** See §3.
6. **Nothing in any of the three repos bears on the OPH proof-chain open items** (G9 record-ΔS bridge, G10 energy ledger, Ward-projected hadronic spectral measure for P, erasure/information-set carrier theorems). PIE's `hexacode.lean` remains the single OPH-relevant DULA artifact, and it was not copied into these repos. See §4.

---

## 1. Repo 1: `DULA-THEOREM---LEAN` — summary table (9 files)

Legend: s = live sorries, a = live custom axioms, nd = native_decide, e? = live `exact?` search tactics left in proofs.

| File | Headline theorem — what it literally states | s | a | nd/e? | Verdict |
|---|---|---|---|---|---|
| Birch–Swinnerton-Dyer Conjecture.lean | Pseudo-Lean (`ord_{s=1}` and `print` are not Lean syntax; `L`, `heightPairing`, `E.rank` undefined). `axiom newform`, **`axiom sha_order : ℕ`** (a bare global natural, later applied as `sha_order E` — type error), `axiom modularity : L(E, s) = L(newform, s)`, **`axiom gross_zagier_kolyvagin`** asserting rank-≤1 BSD *including the exact leading-coefficient formula* (stronger than the actual literature: GZK gives rank equality + Sha finiteness, not the full formula). "dula_bsd_rank_le_one" = unpack of that axiom. Full BSD honestly `sorry`ed as "the Clay Millennium Prize problem". "Rank-5 verification" = `exact (by decide : 5 = 5)`. | 1 | 4 | – | UNBUILDABLE + BROKEN (axioms are the content) |
| Full Birch–Swinnerton-Dyer Conjecture.lean | Near-duplicate of the above (comment wording: "million-dollar" → "Clay Millennium"). | 1 | 4 | – | UNBUILDABLE + BROKEN (duplicate) |
| DULA-BSD.lean | Aristotle's budget-exhausted rescue of the BSD file: defines its own `EllipticCurve` structure, `heightPairing := 0`, polymorphic **`L (x : α) (s : ℂ) : ℂ := 0`**, `Complex.orderZero := 0`, `leadingCoefficient := id`, `sha_order := 0`, `tamagawa_product := 0`. `modularity : L E s = L newform s` proved by `exact?` (it is 0 = 0). GZK demoted to a hypothesis — declared **six times in a row** (session-loop artifact); `dula_bsd_rank_le_one` unpacks its own hypothesis over constant-zero scaffolding. Honest machine comments throughout ("Defining a dummy … to allow the code to compile"). | 0 | 0 | 0/1 | DECORATIVE (all-stub; "BSD" here is arithmetic of constant zeros) |
| DULA Resonator Weight for Twin Primes Modulo 6.lean | Headline `w6_two_exactly_when_admissible`: the weight w6(n) := 1 − (−1)^{φ₆(n)+φ₆(n+2)} equals 2 for every twin candidate (n, n+2 both coprime to 6) — i.e. the trivial congruence fact n ≡ 5 (mod 6), since w6 ∈ {0,2} by construction. No sieve content. Header claims "Fully sorry-free … completely machine-checkable", but proofs use non-Lean syntax (`contradiction using h5`, `rcases p % 6 with 0|1|2|3|4|5`), nonexistent lemmas (`Nat.prod_pow_factorization_eq`, `Nat.factorization_insert`, `Nat.one_mul_mod`), and a Finset.induction whose IH is never bound. | 0 | 0 | – | UNBUILDABLE (repaired content = the classical grading lemma, proven properly in the Goldbach file) |
| DULA for 3D Navier-Stokes Global Regularity conjecture.lean | Aristotle. **`smooth (v) : Prop := True`, `finite_energy := True`, `kinetic_energy := 1`, `no_finite_time_blow_up := True`** — the "global regularity theorem" is `True ∧ True → ∃ sol, True ∧ (∀t>0, ν·1>0) ∧ True`, closed by `aesop`. **No Navier–Stokes equations appear in the file.** Aristotle's header admits it: "simplified definitions … as they were not provided. Proved the conjecture under these simplified definitions." Real bits: `phi6_recovers_congruence` (proven), M6 monoid, ν>0 ∧ E>0 → νE>0. | 0 | 0 | 0/1 | DECORATIVE (vacuous headline, honest annotation) + tiny REAL-MATH mod-6 core |
| DULA for Collatz Conjecture.lean | **A pasted chat transcript, not a Lean file**: prose intro ("Here is the full, no-sorry Lean 4 code…"), closing menu ("Next? … Your move. 🌌💵"). The code uses undefined `v2`, `∞ : ℝ`, `print`, `theorem phi6_is_monoid_hom : MonoidHom …` (a Type, not a Prop), nonexistent `Subtype.monoid`. Headline `dula_collatz_conjecture` (trajectory reaches 1 ∧ total log-drop `< ∞`) is `sorry`, honestly labeled "the open Collatz conjecture in DULA language" — and the "reformulation" adds nothing: total drop finite ⟺ trajectory reaches 1. | 1 | 0 | – | UNBUILDABLE |
| DULA_Theorem.lean | The ψ = θ∘φ grading file (differs from PIE's same-named draft). 0 sorries but cannot compile: `val_mul` stated before any Mul instance exists, `Units ℤ` built with a 2-field anonymous constructor, `fin_cases` on a coercion, imports/lemmas that don't exist (`Mathlib.GroupTheory.Hom.Monoid`, `ZMod.int_cast_add`, `pow_even`, `Int.is_unit_iff`). Content if repaired: (−1)^m factors through m mod 2 — tiny, classical, already proven in PIE's DulaCore. | 0 | 0 | – | UNBUILDABLE |
| dula_goldbach_conjecture.lean | Aristotle; **the one substantive compiled artifact in this repo.** Proven: `n_val_mod_6_eq_neg_one_pow_m5 : (n : ZMod 6) = (−1)^{m₅(n)}` (the grading-recovery lemma, real proof), `phi6_is_monoid_hom` (M6 →* Multiplicative (ZMod 2)), `phi6_recovers_congruence`, and `dula_goldbach_sum_counts_representations` (indicator-product = paired indicator — trivial bookkeeping). Goldbach itself is only **stated** as `def dula_goldbach_conjecture_statement : Prop` (a faithful counting form — never asserted or sorried). Pathology: the same lemma restated **~25×** (`…_v2` … `…_v16`, `…_final` … `…_final_v7`) via `exact?` — a raw prover-session dump. | 0 | 0 | 1/20 | **REAL-MATH** (tiny, classical) + honest conjecture statement; heavy session-dump artifacts |
| DULA as the additive soul of Langlands/GL(1)–GL(2) and BSD.lean | Aristotle. (i) mod-6 grading again (proven). (ii) **The only fresh mathematics in the repo**: for an axiomatized `StrongHeightTheory` class (quadratic scaling + parallelogram law), `heightPairing_bilinear` (⟨aP,bQ⟩ = ab⟨P,Q⟩ by a genuine polarization/induction argument), `heightPairing_symm`, `heightPairing_positive_definite` (non-torsion ⇒ ⟨P,P⟩ > 0). Sorry-free — but conditional on a class with **no instance constructed** (no actual canonical height in the file), and the content is the classical "polarization of a quadratic map is ℤ-bilinear". (iii) Headline: `BSDTheory` class where `analyticRank : ℕ` is a bare field ("we just take it as a given natural number" — honest comment); the instance sets **`L := fun _ => 0` ("Dummy definition") and `analyticRank := 5`; then `theorem dula_bsd_990h3 : … = 5 := rfl`.** The flagship rank-5 claim is rfl on a constant defined as 5. | 0 | 0 | 1/1 | Split: **REAL-MATH** (height-polarization block, conditional) / DECORATIVE headline (self-fulfilling rfl) |

Repo verdict: **no theorem in this repo states-and-proves any named conjecture.** Real content = one classical grading lemma family + one classical polarization lemma family, both Aristotle-proven under axiomatized scaffolding.

---

## 2. Repo 2: `Riemann-Hypothesis-Proof` — summary table (4 Lean sources)

| File | Headline theorem — what it literally states | s | a | Verdict |
|---|---|---|---|---|
| Aristotle proof RH.lean | Aristotle. `Vacuum_Stability_Guaranteed`: **hypothesis** `PrimeInertia_Spectral_Correspondence θ H Op` := "every zero ρ of the Mellin L of kernel θ is 1/2 + iE" — the passed Hermitian operator `Op` **never occurs in the Prop's body** — implies every zero has re = 1/2. Hypothesis literally restates the conclusion; 4-line unpack. Not about ζ at all (arbitrary kernel; `JacobiThetaSum` defined, never used). **Real work: Aristotle disproves the user's claim** — `PrimeInertia_L_Function_divergence_at_neg_one` (θ(iy) ~ y^{−1/2} near 0 via the functional equation ⇒ the s = −1 Mellin integrand is non-integrable) and `PrimeInertia_Generates_Symmetry_False` (the naive global functional equation is false). Honest header: "found it to be false as stated". | 0 | 0 | DECORATIVE headline (hypothesis = conclusion) + **REAL-MATH** refutation lemmas |
| PrimeInertiaEngine.v2.1.lean | Same file as the already-audited PIE copy up to 4 comment lines. Header: "FULLY FIXED & ARISTOTLE-VERIFIED … 100% ready for GitHub. All classical parts are unconditionally verified by mathlib4." Reality: `jacobiTheta₂_modular_S`, `IsSymmetricOn`, `integration_by_parts`, `atBotWithin`, `innerProductSpace.inner (volume.restrict …)` do not exist; `spectrum berryKeatingH` is ill-typed; and `symmetricL_functional_equation` is "proved" by `exact riemannCompletedZeta_one_sub …` — a true Mathlib theorem **about a different object** than their integral `symmetricL` (type mismatch). Axioms: `axiom spectralCorrespondence : ∀ ρ, symmetricL ρ = 0 ∧ ρ ∉ {0,1} → ∃ E, ρ = 1/2 + I·E ∧ E ∈ spectrum berryKeatingH` and **`axiom zeroTorsionCondition : ∀ ρ, symmetricL ρ = 0 ∧ ρ ∉ {0,1} → |re ρ − 1/2| = 0`** — RH-for-this-L verbatim, as an axiom. Both "theorems" are unpacks. | 0 | 2 | BROKEN (axiom = conclusion) + UNBUILDABLE |
| RH_LEAN4_Proof_Aristotle (extensionless; a `.lean` twin was created then renamed away per git log) | Earlier "Author: Grok (rigorous fix of the original Aristotle/Harmonic draft)" version of v2.1. Same two conclusion-bearing axioms; `spectralCorrespondence` here contains **English prose inside the term** ("… ∧ E ∈ spectrum berryKeatingH (on the appropriate self-adjoint extension)") and `h_boundary : ∀ x > 0, deriv (…) x = ... := by sorry` contains a **literal `...`** — neither can parse. Header: "No overclaims. Everything that compiles does so unconditionally" — nothing past the definitions compiles. | 3 | 2 | BROKEN + UNBUILDABLE |
| Riemann_Hypothesis_via_SIO.lean | The "Grand Unified SIO Theory". Headline `Riemann_Hypothesis_via_SIO : ∀ s, zeta s = 0 ∧ 0 < re s < 1 → re s = 1/2` — **genuinely RH-shaped**, but: `zeta`, `zeta_zero`, `nth_prime`, `IsEigenvalue`, `isSelfAdjoint_apply_eigenvalue_im_zero`, `summable_sum_finset`, `adjoint_eq_iff` don't exist; `finset.univ` and `IsO` are Lean 3; `fun L => RMSE_L α M` binds a lambda variable the body never uses (shadowed section variable). 8 live sorries **including sorried definitions** (`eigenvalues_L := sorry`, `quantile_transform := sorry`) and the final proof (`sorry -- Integrated chain implies Re(s) = 1/2`). Decimal-literal machinery feeding "theorems": dula_weight 1.0/0.1, harmonic corrections A₅=0.1, A₇=0.05, A₁₁=0.02, α ≈ 0.2526, C := 1/4 "Prime Uncertainty Principles" (sorried). | 8 | 0 | BROKEN + UNBUILDABLE (real RH statement; proof = sorry resting on curve-fit conjectures) |

**Non-Lean context.** The README ("DULA & Grok/Gemini", "Status: Complete") claims the Monster dimension 196883 = 47·59·71 is "constructed entirely from the Inert Channel" and twin primes are "topological defects (Event Horizons)". The flagship PDF (*A Computational Proof of the Riemann Hypothesis: The SIO Framework and a Prime Physics*, "GROK 4 - GEMINI 2.5 Pro - DULA", 2025-10-13) claims a "definitive computational realization of the Hilbert–Pólya conjecture": the Python scripts build a symmetric Gaussian–Cauchy kernel matrix with mod-6 masks and "fractal/holographic" factors (Sierpinski dimension 1.585, "quantum noise" 0.001), diagonalize, then map eigenvalues onto the first 500 known zeta zeros via a **"computationally discovered transform"** (cubic-spline quantile fit + `curve_fit`), reporting RMSE ≈ 0.3. Fitting a spline to the answer verifies nothing: any increasing spectrum can be so mapped. The companion "Numerical Validation" PDF checks Euler–Maclaurin/Perron numerics on ζ at sample points (fine classical numerics, no RH content). `BSD_RANK_5.html` is a three.js animation: "The saddle is stable. The rank is 5. Birch & Swinnerton-Dyer Conjecture — Visually Destroyed. Five Rainbow Rings = Five Generators of Infinite Order" (ring speeds 1, φ, e, φ², …). The `HOLOGRAPHIC SIO_250k ZEROS.py` header demands "a powerful, multi-GPU supercomputing environment" (750,000² matrix, "~2.25 TB VRAM") — aspirational code.

---

## 3. Repo 3: `Geometric-Propulsion-Hardware`

All ten PDFs are **LLM chat-message exports** (openers like "Eagle eye!", "This is a profound realization.", "You are absolutely right to pause there—"; each ends with a follow-up question to the user, e.g. "Should we integrate this safety logic directly into the 28.87 Hz high-voltage driver script…?"). "3D Printing Hyperbolic Surgery Wedge.pdf" and "OpenSCAD Hyperbolic Surgery Wedge.pdf" extract to identical text.

### (a) What the hardware physically is
An **asymmetric-capacitor corona device — a Biefeld–Brown "lifter"**, and the build docs say so: *"The heart of the device is an Asymmetric Capacitor, following the structural logic of the Biefeld-Brown and Exodus experiments"* (Building the PIE.pdf). Concretely: a high-curvature **needle/wire emitter** at the top port, a large-area **collector mesh/plate** at the bottom, a dielectric ("barium titanate or exotic ceramics") in the "neck" of a 3D-printed hyperboloid wedge (the OpenSCAD model: a cylinder with 45°-rotated cubes as "Leech lattice" ribs and a hyperbolic bore r(z) = (3.70951/2)·√(1+(z/2.887)²)), driven at **20–50 kV** from an **ignition coil / flyback / "solid-state Tesla coil"** switched by an IRFP460 MOSFET via a TC4420 gate driver, pulsed at **28.87 Hz** from an ESP32/Arduino PWM pin. Instrumentation: a torsion balance to detect "Propellantless Thrust (milli-newtons)". That is a standard EHD/ion-wind rig plus a printed trinket.

### (b) Thrust / propulsion / anti-gravity claims, and their basis
Explicit and repeated: "propellantless propulsion system that generates thrust by manipulating the 28.87 Hz Spectral Buffer … momentum transfer without traditional reaction mass" (README); the device will "kick against the Unimodular Volume (V=1) of the Leech lattice"; the Risks PDF discusses the device becoming "a localized gravitational anomaly" and warns of a "permanent topological defect … an Arithmetic Singularity", "massive amounts of heat or Gamma radiation". Basis offered: the PIE numerology constants — **28.87** (in the Lean corpus a bare *dimensionless* literal, `dula_spectral_buffer : ℝ := 28.87`; here it silently acquires units of Hz), δ = 0.1 ("Singularity Lock"), "Li Rigidity 14.504" (= 0.00859884 + 28.87/2 — a decimal plus the numerology constant halved), the Leech q¹ = 0 "no-root miracle", and a "Tao Constant 3.70951" (used interchangeably as a percentage threshold and a wedge radius in mm). No mechanism connects any of this to force generation. **Physics reality:** any milli-newton reading from this build is electrohydrodynamic ion wind plus electrostatic attraction to surroundings — the Biefeld–Brown effect is settled physics (momentum transfer to air ions; zero thrust in vacuum). It is propellant-*ful* propulsion whose propellant is air.

### (c) SAFETY red flags (for a tabletop experimenter)
The real hazards are electrical, and the documents systematically misdirect attention toward fictional ones:
1. **20–50 kV output** from an ignition coil/flyback. Secondary current is limited (tens of mA peak) but delivers painful, involuntary-reaction shocks; genuinely dangerous for anyone with a cardiac condition or implant. The asymmetric capacitor + polarized dielectric **stores charge after power-off** — the driver PDF itself says "discharge probes are used before touching the Surgery Wedge after activation", but specifies no bleeder resistors, no interlock, no current limiting, no one-hand rule.
2. **Unsnubbered inductive switching.** An IRFP460 (500 V rating) hard-switching an ignition-coil primary with no snubber/clamp in the schematic: avalanche failure, exploding package, fire risk. The "wiring definition" is an LLM-generated table, not a tested schematic (28.87 Hz is also far below flyback-transformer operating range — expect erratic HV, core saturation, hot primaries).
3. **Ozone and NOx** from continuous corona/needle discharge indoors; corona onto a **flammable 3D-printed (PLA/PETG) housing**.
4. **EMI**: kV-scale pulsed corona will couple into nearby instrumentation — relevant if operated anywhere near the chi_nu_test electronics.
5. **Mains behind everything**: the 12 V/high-current primary supply is mains-fed; no fusing or isolation is discussed.
6. **The "safety systems" are theater.** The Diagnostics.md "Basel-Tao Stability Monitor" computes `drag = |projected_rigidity − 14.50422|` against threshold `3.709751/1000` and the .sage "diagnostic" checks `28.87/2 + 0.0086 > 14.4` — **comparisons of hardcoded constants that can never detect anything**; the sage script always prints "VACUUM LOCKED (+)" as long as SageMath runs. The "Emergency Shutdown Protocol" addresses runaway black holes (shift frequency away from 28.87 Hz, move δ to 0.5) rather than any electrical hazard. An experimenter following these documents would be encouraged to trust a fake monitor while facing real HV.
7. No laser, no chemicals beyond ceramic dielectrics, no large capacitor bank specified — the stored-energy hazard is the HV capacitance/dielectric itself, not a bank.

**Bottom line: do not power a build from these documents.** If one wants to demonstrate ion wind, well-documented lifter protocols with proper HV supplies, bleeders, and ozone ventilation exist; nothing in this repo adds anything except risk and a printed cylinder.

### (d) Mathematical connection to the OPH chain
None; the pattern is again numerological rhyme. The one real computation in the repo — `DIMENSIONAL_DESCENT.sage` verifying that the q¹ coefficient of E₁₂ − (65520/691)Δ vanishes (the Leech theta series has no norm-2 vectors) — is correct classical modular-forms arithmetic, immediately glued to the tautological `> 14.4` check. The "1, 5 (mod 6) channels" are the same ℤ₂ character as everywhere in the DULA corpus; "octagonal 24 = 8×3 projection", "Property (*) laminar flow" (a real MathOverflow fact about A₂ Voronoi cells), and "Tao 2026 / Guth–Maynard" are real mathematical references name-dropped as machine components. No morphism to OPH's ℤ₆ collar, e^(−P/24), records, or spectral measures is stated or derivable.

---

## 4. OPH proof-chain relevance (all three repos)

Measured against the open items (G9 record-ΔS bridge; G10 energy ledger; Ward-projected hadronic spectral measure for the fine-structure P; erasure-correction/information-set/code-reconstruction carrier theorems; ℤ₆ with derivational content):

- **Erasure/code reconstruction: nothing.** No coding theory of any kind in these repos. PIE's `hexacode.lean` (the one OPH-relevant DULA artifact, see PIE_AUDIT_RAW §3.1) was not copied here and has no successor here.
- **ℤ₆/mod-6: classical rediscovery only.** The grading family (n ≡ (−1)^{m₅(n)} mod 6; φ₆ monoid hom; congruence recovery) is re-proven in three files; the cleanest compiled version is in `dula_goldbach_conjecture.lean`. It is the χ₃ lift = ℤ₂ character, identical in content to PIE's DulaCore family; no ℤ₆-collar action, no e^(−P/24), no new derivational content.
- **G9/G10: nothing.** `dula_energy_dissipation ν E := ν·E` is a one-line product; the SIO "Prime Uncertainty Principles" (Δγ·Δg ≥ 1/4) are sorried statements over decimal-fitted quantities; no records, no entropy, no ledger.
- **Fine-structure P: absent.** No α⁻¹ content in these repos at all; 28.87 reappears only as the hardware "Hz".
- **Reusable (non-OPH) mathematics if anyone wants it:** the height-pairing polarization block (`GL(1)–GL(2) and BSD.lean` — bilinearity/symmetry/positive-definiteness from the parallelogram law, sorry-free, conditional on an uninstantiated class; Mathlib's `QuadraticMap.polar` covers adjacent ground), the grading lemmas in the Goldbach file, and Aristotle's Mellin-divergence refutation in `Aristotle proof RH.lean`.

---

## 5. Red flags (concentrated)

1. **Conclusion-bearing axioms**: `gross_zagier_kolyvagin` (×2 BSD files — and it overstates the literature: the exact rank-≤1 BSD formula is not a theorem as asserted), `zeroTorsionCondition` (RH verbatim as `|re ρ − 1/2| = 0`), `spectralCorrespondence` (×2 files).
2. **`axiom sha_order : ℕ`** — an axiom postulating a bare natural number, then misapplied as a function.
3. **Self-fulfilling flagship**: `analyticRank := 5` … `:= rfl`; `(by decide : 5 = 5)`; "Visually Destroyed" rainbow-ring HTML as "verification".
4. **False certification headers on non-compiling files**: "FULLY FIXED & ARISTOTLE-VERIFIED", "100% ready for GitHub", "Fully sorry-free … completely machine-checkable", "All classical parts unconditionally verified by mathlib4" — same certificate-vs-reality disease as PIE's PDF.
5. **Chat transcripts committed as source**: the Collatz `.lean` ("Your move. 🌌💵"), all ten hardware PDFs, README follow-up questions.
6. **Citing true Mathlib theorems about different objects**: `riemannCompletedZeta_one_sub` "proving" the functional equation of their own integral `symmetricL`.
7. **True-stub tautology**: Navier–Stokes "conjecture" with `smooth := True` and no PDE in the file.
8. **990h3 "rank 5"** is arithmetically impossible at conductor 990 (smallest known rank-5 conductor ≈ 1.9×10⁷); the source call passes six a-invariants.
9. **Session-dump pathology**: one lemma restated ~25× via `exact?`; one hypothesis declared 6×; prover budget-exhaustion notices left in shipped files.
10. **Hardware**: dimensionless 28.87 silently becomes 28.87 Hz; fictional risks (black holes, gamma rays) displace real HV/ozone/fire hazards; "safety monitors" compare hardcoded constants and cannot trip.

## 6. Genuinely reusable mathematics found (all classical, all modest)

- `dula_goldbach_conjecture.lean` — n ≡ (−1)^{m₅(n)} (mod 6), φ₆ monoid hom, congruence recovery (Aristotle-proven; 1 native_decide, 20 `exact?`).
- `GL(1)–GL(2) and BSD.lean` — polarization of an axiomatized canonical height is symmetric, ℤ-bilinear, positive-definite off torsion (sorry-free; class never instantiated).
- `Aristotle proof RH.lean` — Mellin-integral divergence at s = −1 for weight-1/2 kernels and refutation of the naive global functional equation (Aristotle's honest negative result).
- `DIMENSIONAL_DESCENT.sage` — Leech theta q¹ = 0 via E₁₂ − (65520/691)Δ (correct Sage computation, wrapped in a tautological "diagnostic").

None of these touches any OPH proof-chain open item.
