# `formal/` — machine-checked closure work for the OPH minimal proof chain

| | |
|---|---|
| Date | 2026-07-06 (v3 + v4 campaigns) + 2026-07-07 (v5 + v6 + v7 + **v8** campaigns) |
| What | A standalone Lean 4 + Mathlib project that (v3) formalized the `OPH_CORE_MINIMAL_PROOF_CHAIN.md` §7 targets and the "open jewel", (v4) **absorbed the OPH Lean core itself into this tree with its three `sorry`s discharged** and formalized the remaining mathematically-formalizable links: the L2.4 algebra (hypercharges + ℤ₆), the D5 Einstein-branch algebra, the dark-sector mathematics (L2.7/L2.8 + the L2.11 FTC step), the collar-gate skeleton (L2.6), the ΔS-bridge definition side (P5/L2.12), the §7.6 stretch item (general Rule-90 decodability: boost invariance + the parity obstruction), and the hexacode MDS toy (§9), and (v5) **settled the odd-cylinder gap-2 threshold** (the §7.6 item v4 left open — the complete parity classification), **built the D3 finite modular core** (retiring "the one link with no isolable finite-mathematics core"), closed the hexacode weight distribution, machine-checked the run-matrix conversion constants (finding and fixing one ledger erratum), and formalized the corpus appendix's QBFT safety theorem (finding a boundary caveat), and (v6) **settled the stride conjecture** — T25, the coprimality classification of two-column screens (`gcd(g,n) = 1 ∧ n ≤ 2(t+1)` ⟺ capacity), via the new mirror lemma and quotient lift — plus the separation-statement clauses and the `EdgeRepairable`-strictness witness, and (v7) **closed the adversarial audit's remaining mathematics**: T27 — Route A *assembled* (local tube-preserving transactional decode-repair + `H_B` + the sharp `H_fib` jointly on the T9′ carrier, with both impossibility negatives and the stall witness machine-checked, closing F2); T28 — the real-time modular flow (`H = −log ρ` exists; `σ_z` is a ⋆-automorphism group with the textbook KMS boundary condition; `σ_i = Δ_ρ`; Hamiltonian-implemented uniqueness `e^{−K} = c·ρ`, closing F9 finite-dimensionally); T29 — the channel bridge (one indexed family, both counters, composite Tier-B1 law, closing F11); the ℤ₆ kernel as a **group isomorphism** (F10c); and the theorem-grade energy anchors bracketing the G10-convention (F15). **The whole chain lives, machine-proven, in this one directory tree.** The **v8 campaign** (same day, verification pass + simulation feedback + leftovers): independently re-verified the v7 modules (read + build + 1199-declaration environment sweep), then closed T27's named leftover — **T32, arbitrary-schedule termination** (`decodeStep_wellFounded`: the decode dynamics is strongly normalizing under EVERY scheduler; the rank stratification itself is the lexicographic potential) — and T28's named leftover — **T33, Skolem–Noether for the matrix algebra** (`algEquiv_matrix_inner`: every `ℂ`-algebra automorphism is inner; `kms_algEquiv_structure`: any KMS-satisfying automorphism IS the modular map with conjugator `c·ρ`) — plus the F24 residue — **T35, `TriangulatedSphere`** (the twelve-port count's three assumed equations are now theorems of an actual surface structure; kernel-checked icosahedron instance) — and two theorems surfaced by the simulation companion (`oph_sim/FINDINGS.md`): **T31, the readout trichotomy** (`readout_trichotomy` + `tubeData_surjective_iff`: ghosts/bijective/unrealizable split exactly at the threshold — above it EVERY tube reading is realizable, at it the readout is a bijection, below it empty fibers exist ⟺ `n < 2(t+1)`; Route-A corollaries `exists_unrealizable_tube_iff`, `no_stall_at_threshold`) and **T30, the local-decodability phase boundary** (`spread_screen_inferable_iff`: screens with pairwise column distance ≥ 3 admit ZERO local constraint-propagation inferences at any horizon; `adjacent_closure_complete`: the adjacent tube's propagation closure is the whole block at the sharp threshold; `violet_exhibit`: `n=8, g=3, t=3` is machine-checked as determined-but-locally-dark). **T34-lite**: the sloped-screen *definition* is now formal (`slopeTube`, floor convention, provably slope-0 = T9's tube), the failure half is a theorem at every slope, and slopes 1/2, 1/3, 2/3 have kernel-checked threshold instances — the general positive half remains the named open conjecture. |
| Toolchain | `leanprover/lean4:v4.29.1`, Mathlib pinned to the **same revision** as `observer-patch-holography/LEAN` (`5e932f97…`), so results transfer 1:1 (upstreaming = file move + namespace) |
| Status | **Builds clean. 0 `sorry` (including the imported core — its three source `sorry`s are discharged here), 0 custom axioms, no `native_decide`.** Every headline theorem ends with `#print axioms`; all report at most `[propext, Classical.choice, Quot.sound]` |
| Build | `cd formal && lake exe cache get && lake build` (first run downloads the Mathlib cache) |
| Provenance | Files under `OPHProofChain/Core/` are attributed copies from `observer-patch-holography/LEAN/` (headers state exactly what changed; all modifications carry `[formal-v4]` tags). `HexacodePort.lean` is an attributed port from `dula/prime-inertia-engine` (header; the v5 weight-distribution section is native, `[formal-v5]`-marked). `Rule90Stride.lean` and `RepairHypotheses.lean` are native `[formal-v6]` modules; `RouteA.lean`, `ModularFlow.lean`, and `ChannelBridge.lean` are native `[formal-v7]` modules; `Rule90Readout.lean`, `Rule90Propagation.lean`, `Rule90Slope.lean`, and `SimplicialSurface.lean` are native `[formal-v8]` modules (the first three crediting `oph_sim/FINDINGS.md` for surfacing the statements). `ConsensusSafety.lean` formalizes (with attribution) the corpus appendix `paper/appendix_B_bft_qecc_extensions.tex`. Everything else originates in this repo. |

## What was closed, in one table

| Module | Result | Was (proof chain v2) | Is now |
|---|---|---|---|
| [`QuotientRepair.lean`](OPHProofChain/QuotientRepair.lean) | **P1** — the full quotient repair operator package: presentation `(Σ,Γ,q,Q,C_Q,B,μ,𝖠,≺_𝖠)` with `H_B, H_↓, H_◇, H_comp`; `locRep` (least enabled transaction); `globalRepair` total, valued in `C_Q`, boundary-preserving, idempotent, fixed exactly on `C_Q`; **schedule independence** via a self-contained Newman's lemma; `World`, `world_is_fixedPt`, `repair_respects_gauge` (3 forms); a real demo instance; and the separation package `symmetricPair_not_locallyConfluent` + `symmetricPair_descends` + `symmetricPair_normalForm_iff` (`[formal-v6]`) showing `H_◇` does genuine work — the symmetric witness honestly satisfies `H_↓ ∧ H_comp` (with `H_B` vacuous) yet is not locally confluent | paper-side theorem (tex 433–610, 1143–1170); Lean core carried 3 `sorry`s for exactly this machinery; `PROOF_INDEX.md` at 0 % of Prop 4.2 | **machine-checked** — every Def 4.1 / Prop 4.2 gap row of `PROOF_INDEX.md` has a corresponding sorry-free theorem here (mapping table in the module docstring) |
| [`NotEinsteinComplete.lean`](OPHProofChain/NotEinsteinComplete.lean) | **P2** — `bare_consensus_not_einstein_complete`: two geometric extensions of the *same* (real, non-degenerate) consensus reduct with opposite Einstein truth values; no predicate of the reduct decides the Einstein equation | paper-side negative theorem (tex 227–271) | **machine-checked** (definability separation, matching the paper's own proof) |
| [`LayeredCarrier.lean`](OPHProofChain/LayeredCarrier.lean) | **P3** — layered functional boundary carrier: `extend` (=`E(b)`), staged sweep, `H_B` (`sweep_restrictB`), reconstruction (`sweep_eq_extend`), `H_fib` singleton fiber (`hfib_singleton`), presentation-free reconstruction corollary, genuinely multi-edge demo | paper-side theorem (tex 1225–1364), feed-forward class (review R1) | **machine-checked**, same honest scope (feed-forward class — the QECC-strength content lives in the next row) |
| [`Rule90Cylinder.lean`](OPHProofChain/Rule90Cylinder.lean) | **The open jewel (§7.2 / R1), proven sharp**: on the Rule-90 `n`-cylinder run for `t` steps, the width-2 timelike tube is an information set **iff `n ≤ 2(t+1)`** (`tube_information_set_iff`) — the sideways-lightcone bound and the raw counting bound coincide, so the timelike screen **saturates the information bound**. Plus: width-1 columns fail at *every* horizon (mirror-kernel construction, `n ≥ 3`; nilpotency, `n = 2`), and **no proper spacelike subset** of the initial row ever works — a sharp timelike/spacelike asymmetry | named open (review R1: "remains the open jewel"); only the width-3 one-step toy existed | **machine-checked, with a sharp iff** — strictly stronger than what was asked (the ask was sufficiency on some family) |
| [`LambdaConstancy.lean`](OPHProofChain/LambdaConstancy.lean) | `[formal-v6.1]` **T26 — the cosmological-constant step**: `einstein_equation_with_constant` — null-cone matching at every point of a connected discrete chart + the contracted Bianchi identity + local stress conservation (both **named** hypotheses) ⟹ `F = κT + Λη` with one constant `Λ`; `lambda_not_constant_without_connectivity` shows connectivity is load-bearing | the adversarial audit's F8: the pointwise λ of `jacobson_step` was called "the cosmological constant" one (written, unformalized) Bianchi step early | **machine-checked** — T14 now ends at the Einstein equation, with the geometry/physics inputs named |
| [`RepairHypotheses.lean`](OPHProofChain/RepairHypotheses.lean) | `[formal-v6]` **The T12 hypothesis lattice, closed**: `rule90_edgeRepairable` + `rule90_not_frustrationFree` + `edgeRepairable_strictly_weaker` — the width-3 carrier witnesses that `EdgeRepairable` (T12's completeness hypothesis) is **strictly** weaker than `FrustrationFree`, so canonical completeness provably covers carriers where no frustration-free operator exists | the docstrings asserted "strictly weaker" prose-only | **machine-checked** (native module; `Core/` copies stay verbatim) |
| [`CarrierBridge.lean`](OPHProofChain/CarrierBridge.lean) | The jewel in the Lean core's own vocabulary: `rule90Cylinder n t` (a `(t+1)`-patch, `t`-edge `OPHCarrier`), `H_fib` discharged for the tube boundary in the exact `boundary_fiber_observer_unique` binder form, sharpness, width-1 failure, and proof that the tube is strictly coarser than `obsMap` (no definitional endpoint) | R1: "proper-subset boundaries on carriers with code redundancy — remains the open jewel" | **machine-checked** on a genuinely multi-edge carrier |
| [`EnergyCage.lean`](OPHProofChain/EnergyCage.lean) | **G10, theorem side**: the ABBA cycle identity (`cycleWork = toggleCost q₁ − toggleCost q₂`), `no_free_toggle` (zero toggle ledger ⇒ zero extractable work — else perpetual motion), ledger lower bound; **design-point arithmetic** machine-checked: `σ_ph ∈ (11.6, 11.8) kg/m²` per `10⁻⁹`, toggle transaction `∈ (3.49, 3.52) MJ` at 56 gf | §5 prose + hand arithmetic; "valid as an external physics obligation" (their grade) | **machine-checked** (the *cage*; the force law itself, L2.11, remains open — this prices it) |
| [`PBranches.lean`](OPHProofChain/PBranches.lean) | **The two-P finding (review §P) + the L2.10 value**: `Ppub = φ + √π/137.035999177 = 1.630968209403959…` digit-checked from scratch (`√5`, 20-digit `π` bounds); solver root differs by `3.8–4.0×10⁻⁶`; `χ_can = e^(−P/24) = 0.9343006…` via a 6-term Taylor sandwich with explicit remainder; branch discrepancy `Δχ ∈ (1.4, 1.6)×10⁻⁷` — real in the 8th digit, immaterial for the experiment | prose adjudication + executed code | **machine-checked arithmetic**, with the CODATA provenance carried in the definition (nothing here derives α — see module docstring) |
| [`ScalarResponse.lean`](OPHProofChain/ScalarResponse.lean) | **P4, form half (linear-algebra core)**: under SEE (one-generator register), every linear response is `S = c·η ↦ χ·c`, and no second susceptibility exists | paper-side theorem under SEE (screen tex 817–864) | LA core **machine-checked**; SEE itself stays the named physical hypothesis (unchanged, on purpose) |
| [`Rewriting.lean`](OPHProofChain/Rewriting.lean) | Self-contained Newman's lemma + unique normal forms (independent re-proof, compatible with the core's `AbstractRewriting.lean`) | (infrastructure) | machine-checked |

## The v4 campaign, in one table

| Module | Result | Was (proof chain v3) | Is now |
|---|---|---|---|
| [`Core/Primitives.lean`](OPHProofChain/Core/Primitives.lean) (+ `Core/AbstractRewriting.lean`, `Core/Rule90.lean`) | **The OPH Lean core, imported and discharged.** `localRepair` := the canonical frustration-free snap operator (fires iff a broken incident edge exists ∧ the patch can satisfy all its interfaces; `Classical.choose` state); `Repair` := the declared-order least-firing-site iterate (Route B on the original carrier); `repair_respects_gauge` **proved** (everything the operator reads factors through `obsMap`). Bonus: the file's own `Termination` + `LyapunovDescent` proved for **every** carrier, `Completeness` under the named `EdgeRepairable` (weaker than frustration-free), the file's own `Confluence` **refuted** on `demoCarrier`, and `localRepair = demoLR` there (anti-degeneracy) | the three documented `sorry`s lived in the OPH team's repo; `QuotientRepair.lean` proved their content in quotient form; "upstreaming is mechanical; their call" | **the whole chain machine-proven in this one tree, 0 `sorry`** — the copies are attributed, all modifications `[formal-v4]`-tagged |
| [`Hypercharge.lean`](OPHProofChain/Hypercharge.lean) | **L2.4 algebra, half 1**: Yukawa closure + `[SU(2)]²U(1)` + gravitational anomaly ⇒ the hypercharge ratios (any `N`, over `ℚ`); `[SU(N)]²U(1)` implied by Yukawa alone; `[U(1)]³` cancels **identically** on the ray; `Q(ν_L) = 0` pins `Y_Q = 1/(2N)`; `N = 3` ⇒ the exact SM lattice, uniquely | "real algebra" (prose grade), unformalized | **machine-checked** |
| [`CenterZ6.lean`](OPHProofChain/CenterZ6.lean) | **L2.4 algebra, half 2**: in `ℤ₃ × ℤ₂ × ℝ/ℤ`, the subgroup acting trivially on all six SM multiplets is **exactly** `⟨(1,1,1/6)⟩ ≅ ZMod 6` — since v7 as a genuine **additive-group isomorphism** (`kernelAddEquiv`, audit F10c), not merely a bijection; the generator has order 6; charges match `Hypercharge.smAssignment` | compact paper Prop `prop:z6`, prose | **machine-checked** (MAR — the *selection* of the package — stays the named physical hypothesis) |
| [`EinsteinBranch.lean`](OPHProofChain/EinsteinBranch.lean) | **L2.3 linear-algebra core (D5)**: the rest-frame arithmetic of `thm:einstein`; the timelike polynomial upgrade (symmetric tensor vanishing on all future unit timelike directions is 0 — finite witnesses, no analysis); the classic null-cone lemma (`B = λη`, the Jacobson equation-of-state step); the packaged conditional: variational identities (named physics) ⇒ full tensor Einstein equation | conditional tex chain (D4–D5), algebra unformalized | **LA core machine-checked**; entropy stationarity / small-ball / area-variation identities stay named hypotheses |
| [`DarkSector.lean`](OPHProofChain/DarkSector.lean) | **L2.8 + L2.7 mathematics**: activation law well-definedness, monotonicity, Newtonian + deep-MOND limits (exact `√(a_eff g_b)` scaling), BTFR, the rare-event `(1−μ/m)^m → e^{−μ}`; the phantom-density identity as pure bookkeeping over any linear divergence; the exact point-source profile `M_A′ = 4πr²ρ_A` + positivity; the thin-device FTC step `F = g²/(4πG)·A·χ·ΔS` | compact conditional derivation + Correction Audit, prose | **mathematics machine-checked**; Poisson-counting premises + flux closure + the lab response law stay named physics |
| [`CollarGate.lean`](OPHProofChain/CollarGate.lean) | **L2.6 skeleton**: finite-slice collar model; slice-wise unbiasedness ⇒ `λ_collar = e^(−P/24)` exactly (the gate theorem's own proof); the Jensen band `e^(−P/24) ≤ λ ≤ 1` under the mean condition; `χ_can = λ_collar` forcing; the `24 = (P/4)/6`-per-class and `2×12`-oriented-slots bookkeeping; discrete Gauss–Bonnet: any all-triangle sphere has total defect 12 ⇒ exactly 12 unit-defect ports | conditional theorem with explicit gate (L1–L7) + receipts, prose | **skeleton machine-checked**; the L1–L7 operator-algebra clauses stay named physics |
| [`DeltaSBridge.lean`](OPHProofChain/DeltaSBridge.lean) | **P5 / L2.12 definition side**: the finite coherent-source generator (Def B.5) as a formal object; the exact increment identity `(𝓛 𝒩)(q) = S·A(q)` (Theorem B.7) + positivity + non-saturation; the Tier-B response-law shape | "definition side closed on paper; **no formal object to port yet**" | **the formal object exists and the identity is machine-checked**; G9 (the numerical record-ΔS → gravity-ΔS map) stays open — a null bounds only `χ·ΔS` |
| [`Rule90Decoding.lean`](OPHProofChain/Rule90Decoding.lean) | **The §7.6 stretch item**: general `IsInformationSet` framework (vanishing-form equivalence, decidability, universal counting bound, monotonicity); T9 restated; **NEW — boost invariance**: the *lightlike* width-2 screen is an information set iff `n ≤ 2(t+1)`, the same sharp threshold (one-sided double-speed sweep); **NEW — adjacency is load-bearing**: the gap-2 screen on even cylinders fails at every horizon (explicit alternating-seed kernel); decided odd-`n` and boundary instances | "generalize T9 … a full decodability classification" (stretch, open) | **width-2 geometry settled completely; arbitrary subsets now a decidable predicate** (full weight-distribution classification honestly still open) |
| [`HexacodePort.lean`](OPHProofChain/HexacodePort.lean) | **The §9 suggestion**: the `dula/` hexacode ported (attributed); its own open list closed in Lean — minimum distance 4 (⇒ `[6,3,4]₄`, MDS), Hermitian self-duality — and the OPH payoff: **every 3-subset of coordinates is an information set** (proved from `d = 4`, not by enumeration) | "one `decide` lemma away" (suggestion) | **machine-checked**; the geometry-blind MDS extreme next to Rule-90's geometry-sensitive screens |

## The v6 campaign, in one table

| Module | Result | Was (proof chain v5) | Is now |
|---|---|---|---|
| [`Rule90Stride.lean`](OPHProofChain/Rule90Stride.lean) (new, native) | **T25 — the coprimality classification of two-column screens**: `gapTube_isInformationSet_iff` — the screen `{j₀, j₀+g} × [0,t]` is an information set **iff `gcd(g,n) = 1` and `n ≤ 2(t+1)`**. Built on the **mirror lemma** (`mirror_of_column_dark`: a dark column forces mirror symmetry about itself — the defect trajectory is Rule-90 in displacement space, dark on an adjacent pair) and the **quotient lift** (`traj_comap`: reduction mod `gcd` is a covering; both columns land on one dark quotient column). T9-sufficiency, T20, and the width-1 negatives re-derived as the `g = 1, 2, 0` corollaries; boundary `decide`s at `(8,3,3)`/`(8,3,2)`/`(9,3,4)` | §7 item 6's *conjecture* ("capacity iff `gcd(g,n) = 1`", from T20's mechanism) | **machine-checked, sharp, complete** — and stronger than conjectured: full adjacent capacity at the same threshold for every coprime stride |
| [`QuotientRepair.lean`](OPHProofChain/QuotientRepair.lean) (extension, `[formal-v6]`) | `symmetricPair_descends` + `symmetricPair_normalForm_iff` — the separation witness satisfies `H_↓` and `H_comp` honestly (`H_B` vacuous on the boundary-free carrier) | the separation statement's non-confluence half only | **all clauses machine-checked** (`normalForm_iff` axiom-free) |
| [`RepairHypotheses.lean`](OPHProofChain/RepairHypotheses.lean) (new, native) | `edgeRepairable_strictly_weaker` — the width-3 carrier is `EdgeRepairable` but not `FrustrationFree` | "strictly weaker" asserted in docstrings/docs, prose-only | **the strictness is a theorem** — T12's completeness provably covers non-frustration-free carriers |

## The v7 campaign, in one table

| Module | Result | Was (proof chain v6.1) | Is now |
|---|---|---|---|
| [`RouteA.lean`](OPHProofChain/RouteA.lean) (new, native) | **T27 — Route A assembled** (holes-audit F2): local decode transactions (single-cell writes, edge-bounded windows, formula-mismatch trigger) under a declared responsibility roster (budgets `R = min t (n−2)`, `L = (n−2)−R` — a roster with both budgets `≤ t` exists **iff** `n ≤ 2(t+1)`, the jewel's own threshold); liveness (`pass_spec`), `H_B` (`decodeStep_tube`), **observer uniqueness in equality form under any schedules** (`routeA_observer_uniqueness`), completeness ⟺ fiber realizability (`routeA_world_consistent_iff`), the bundle with the sharp `H_fib` (`routeA_assembled`); **negatives**: `rule90CylinderOPH_no_frustrationFree_repair` (∀ n ≥ 1, t ≥ 1), `canonical_repair_stalls` (the audit's exact witness), `stallRecord_tube_unrealizable` (its fiber has no consistent record — the stall is logic) | F2 acknowledged in place; "the concrete v7 formalization target"; stall witness "checkable by hand, not in tree" | **machine-checked, jointly, on one carrier — the composition the program's flagship story always claimed** |
| [`ModularFlow.lean`](OPHProofChain/ModularFlow.lean) (new, native) | **T28 — the real-time modular flow** (holes-audit F9): `exists_modularHamiltonian` (`H = −log ρ`, spectral construction); `flow` = `e^{izH}(·)e^{−izH}` for all complex `z` — group law, ⋆-automorphisms, unitary propagators at real times, norm-continuity, state invariance; **`flow_I_eq_modular`** (`σ_i = Δ_ρ` — the analytic anchor); **`kms_boundary`** (the textbook KMS condition with real time inside); **`hamiltonian_kms_unique`** (`e^{−K} = c·ρ`, real `c > 0`) | "the real-time statement named as open (§7 item 7)"; T21's map "iterated over integers" | **the clock is a clock, machine-checked**; named leftover: Skolem–Noether (beyond Hamiltonian-implemented flows); BW identification stays physics |
| [`ChannelBridge.lean`](OPHProofChain/ChannelBridge.lean) (new, native) | **T29 — the channel bridge** (holes-audit F11): `Channel` (one finite indexed family, both panels), `toRegister`/`toSlices`, **`same_family := rfl`**, `bridge_gate`, **`channel_composite`** (`λ_collar·(𝓛𝒩)(q) = e^{−P/24}·S·A(q)`), non-vacuity | "the same counter" was prose across two disjoint modules; v6.1 demoted the wording | **the identification is definitional inside one structure; the composite Tier-B1 law is a theorem**; residues = the named channel identification + G9 |
| [`CenterZ6.lean`](OPHProofChain/CenterZ6.lean) (extension, `[formal-v7]`) | `phase_add/zero/neg`, `kernelSubgroup`, **`kernelAddEquiv : ZMod 6 ≃+ kernelSubgroup`** | set bijection + `addOrderOf g0 = 6` (audit F10c: "assemble-able but never assembled") | **the group isomorphism the documents quote** |
| [`EnergyCage.lean`](OPHProofChain/EnergyCage.lean) (extension, `[formal-v7]`) | `cycleWork_self` (fixed-position ⇒ zero forced entry), `bench_cycle_work_value` (`(0.549, 0.550) J` per metre), `mass_energy_value` (`(5.03, 5.04)×10¹⁵ J`), **`anchor_ordering`** (floor < G10-convention < ceiling, strict) | the anchors stated in prose (v6.1's F15 fix) | **interval arithmetic — the audit's "seven orders above, nine below" is now a theorem** |
| [`EnergyCage.lean`](OPHProofChain/EnergyCage.lean) (extension 2, `[formal-v7]`) | **the cage slogan at full generality** (holes-audit F25): schedules as lists of moves/toggles; the first law along any schedule; **`no_schedule_beats_the_ledger`** (closed schedules: work = net ledger ≤ toggles·ε) | slogan proven for the ABBA cycle only | **every schedule covered; ABBA is the two-toggle case** |
| [`CarrierBridge.lean`](OPHProofChain/CarrierBridge.lean) (extension, `[formal-v7]`) | **`hfib_strictly_weaker_than_informationSet`** (holes-audit F20): the row-1 readout of `rule90Cylinder 3 1` satisfies the `H_fib` binder verbatim while two distinct consistent records share it | the audit's counterexample, compiled out-of-tree | **in-tree; the paper's front-door "exactly" corrected to the true direction** |
| [`LambdaConstancy.lean`](OPHProofChain/LambdaConstancy.lean) (extension, `[formal-v7]`) | **`lambda_constant_symm`** (holes-audit F23): constancy under symmetric-closure connectivity | forward root-reachability only (ℤⁿ-style charts uncovered) | **covered** |
| [`RepairHypotheses.lean`](OPHProofChain/RepairHypotheses.lean) (extension, `[formal-v7]`) | **`frustrationFree_properly_within_edgeRepairable`** (holes-audit F29(v)): the proper class inclusion, packaged | a one-meta-step gloss over a conjunction | **packaged theorem** |
| [`evidence/`](evidence/) (new) | **the computational-evidence artifacts** (holes-audit F22): `decodability_checker.py` + committed output tables — the stride sweep (`n ≤ 28`) and the slope sweep (`n ≤ 20`, floor convention **defined**) | two "machine experiment" claims with no artifact in the repo | **receipts committed; both claims re-verified** |

## The v5 campaign, in one table

| Module | Result | Was (proof chain v4) | Is now |
|---|---|---|---|
| [`Rule90Decoding.lean`](OPHProofChain/Rule90Decoding.lean) (extension, `[formal-v5]` section) | **T20 — the complete parity classification of the gapped screen**: `gapTwoTube_isInformationSet_iff_parity` — the gap-2 width-2 screen is an information set **iff `n` is odd and `n ≤ 2(t+1)`**. Odd cylinders: *full adjacent capacity at the same sharp threshold* (middle-column reconstruction + two fans zero row 1; the descent to the seed is algebraic — the distance-2 walk is transitive exactly on odd cycles). Even cylinders: never (v4's checkerboard + the degenerate `n = 2` case) | "the general odd-`n` gapped-screen threshold is left open" (v4's honest-scope note; only the `gapTwo_five_two` decided instance) | **machine-checked, sharp, complete** — the last width-2 question closed; tilt is irrelevant, separation matters exactly through cycle parity |
| [`ModularCore.lean`](OPHProofChain/ModularCore.lean) | **T21 — the D3 finite (type-I) modular core**: `kms` (existence — the algebraic KMS identity for `Δ_ρ = ρ·ρ⁻¹` under `ω = tr(ρ·)`), **`kms_unique`** (*any* map satisfying the KMS identity equals `Δ_ρ` — no linearity assumed), automorphism + flow structure, `modular_eq_id_iff_tracial` (every non-tracial state ticks), the faithful-state axioms discharged (`state_one/star/nonneg/faithful`), and the ticking-qubit witness | "the one Layer-2 link with no isolable finite-mathematics core (Tomita–Takesaki/Bisognano–Wichmann)" — fully open tex | **the finite core exists and is machine-checked**; what stays physics is exactly the BW identification (modular flow = boosts) + the scaling limit — D3 is now graded like every other Layer-2 link |
| [`HexacodePort.lean`](OPHProofChain/HexacodePort.lean) (extension, `[formal-v5]` section) | **T22 — the full weight distribution** `A₀ = 1, A₄ = 45, A₆ = 18` (enumerator `x⁶ + 45x²y⁴ + 18y⁶`), kernel-`decide`d | the source file's Python-checked numbers; v4 closed only `d = 4` + self-duality | **every hexacode claim of the `dula/` source file is now closed in Lean** |
| [`ConsensusSafety.lean`](OPHProofChain/ConsensusSafety.lean) | **T23 — the QBFT safety core** (corpus appendix B, attributed): quorum-overlap counting ⇒ no conflicting finalizations (`qbft_safety_core`, composed `qbft_safety` at `n = 3f+1`), the general-`n` sizing, **and the boundary finding `quorum_overlap_gap`**: the appendix's "A6 guaranteed by A3" fails for `n > 3f+1` at fixed quorum `2f+1` | written proof sketch in the appendix, unformalized (outside the chain, inside the corpus) | **machine-checked, with a formal caveat the prose missed** — liveness/optimality stay external citations (DLS88, LSP82), honestly |
| [`LedgerNumerics.lean`](OPHProofChain/LedgerNumerics.lean) | **T24 — run-matrix conversion constants theorem-form**: `C_geom` to 7 digits, `C_geom·A`, the headline `Δν_min ∈ (9.084, 9.085)×10⁻¹⁷` (print 9.1 rounds up — safe), design point (0.550 N, 56.12 gf, SNR 1.10×10⁷), the lock-in floor **exactly** `10⁻⁶/30` (`√(2/1800) = 1/30`), and the battery-coupon ceiling `∈ (1.02, 1.03)×10⁻¹¹` | hand arithmetic in DOCUMENT C; DOCUMENT A printed the coupon ceiling as "≲ 1×10⁻¹¹ (≈ 0.5 gf)" | **machine-checked; one erratum found and fixed** — the coupon ceiling was understated ~3 % in the unsafe direction; DOCUMENT A §1.9 now prints `≲ 1.1×10⁻¹¹ (≈ 0.58 gf)` with an erratum note |

## The v8 campaign, in one table

| Module | Result | Was (proof chain v7) | Is now |
|---|---|---|---|
| [`RouteA.lean`](OPHProofChain/RouteA.lean) (extension, `[formal-v8]`) | **T32 — universal termination**: `misMeasure` (lexicographic stratum-mismatch counts) strictly decreases under every accepted transaction ⟹ `decodeStep_wellFounded` (strong normalization), `no_infinite_decode_run`, `exists_normalForm_extension`, `routeA_universal_settlement` (every schedule terminates AND lands on the tube-pinned record) | "honest leftover: arbitrary-schedule termination (uniqueness already covers every terminating schedule)" | **no scheduler can run forever — the roster names a repair, it is not needed for repair to terminate** |
| [`Rule90Readout.lean`](OPHProofChain/Rule90Readout.lean) (new, native) + [`RouteA.lean`](OPHProofChain/RouteA.lean) corollaries | **T31 — the readout trichotomy** (from `oph_sim/FINDINGS.md` item 10, sharpened): `tubeData_surjective_iff` (`2(t+1) ≤ n` ⟺ every tube reading realizable — kernel count through the first isomorphism theorem, fans supplying the kernel bound), `tubeData_bijective_iff` (`n = 2(t+1)` — the double extreme), `readout_trichotomy`; Route-A landing: `exists_unrealizable_tube_iff` (`n < 2(t+1)` ⟺ empty fibers exist = T27.4's regime), `all_tubes_realizable`, `no_stall_at_threshold` | the bits/unknowns table was a simulation observation; the `n > B` row said only "ghosts exist" | **machine-checked and stronger: above threshold every reading is realizable (ghosts, no empty fibers); at threshold bijective; below, stalls possible — the stall dichotomy has content exactly below the jewel's threshold** |
| [`Rule90Propagation.lean`](OPHProofChain/Rule90Propagation.lean) (new, native) | **T30 — the local-decodability phase boundary** (from `oph_sim/FINDINGS.md` items 1–3): `Inferable` (the three directed readings of the constraint) + `inferable_sound`; `spread_screen_inferable_iff` — screens with pairwise column ring-distance ≥ 3 have propagation closure = themselves (**zero** local inferences, ever); `adjacent_closure_complete` — the tube's closure is the whole block at the threshold; `tube_information_set_via_propagation` — T9 re-derived through the closure; **`violet_exhibit`** — `n=8, g=3, t=3`: information set (T25) with zero locally-derivable bulk cells | the sim's stall scan found 212 determined-but-locally-dark configs, ring-distance ≥ 3 in every one; the two-line argument lived in FINDINGS prose | **determination and local derivability provably split; the sim's flagship violet cell is a theorem** (the gap-2 crawl's completeness is the named leftover) |
| [`ModularFlow.lean`](OPHProofChain/ModularFlow.lean) (extension, `[formal-v8]`) | **T33 — Skolem–Noether, finite dimension**: `algEquiv_matrix_inner` (every `ℂ`-algebra automorphism of `Matrix ι ι ℂ` is inner — classical intertwiner construction, `U·e_j := φ(E_{j i₀})·w`); `kms_algEquiv_structure` (any KMS-satisfying automorphism IS the modular map, inner with conjugator `c·ρ`, `c ≠ 0`) | "extending beyond Hamiltonian-implemented flows needs Skolem–Noether — mathematically standard, not included here" | **the Hamiltonian form is provably generic — there is no non-inner automorphism a rival clock could use**; BW + scaling limit stay physics |
| [`SimplicialSurface.lean`](OPHProofChain/SimplicialSurface.lean) (new, native) | **T35 — a surface for the twelve ports** (holes-audit F24 residue): `TriangulatedSphere` (faces, edges, closedness, Euler = the named topological input); `edges_eq_biUnion` (the edge set is forced); **`three_faces_eq_two_edges` and `degree_sum_eq_two_edges` PROVEN** by double counting (they were `sphere_defect_count`'s assumed equations); `defect_count`/`twelve_ports` consume the v5 theorems unchanged; `ofFn` indexed constructor + kernel-checked `icosahedron` (20/30/12, all degrees 5, `icosahedron_ports`) | `sphere_defect_count` consumed three assumed equations over bare `(V,E,F,deg)`; "any all-triangle closed surface" described absent structure | **the equations are facts of a surface; L0 (that the collar IS such a complex) unchanged as the named physics** |
| [`Rule90Slope.lean`](OPHProofChain/Rule90Slope.lean) (new, native) | **T34-lite — sloped screens pinned down**: `slopeTube` (floor convention — the committed sweep artifacts' definition, now formal), `slopeTube_zero_eq_tubeSet`, `slopeTube_not_informationSet` (the failure half at EVERY slope, free from counting), kernel-checked threshold instances at slopes 1/2, 1/3, 2/3 (`n = 7, 8, 10`) | the slope conjecture lived in prose + evidence artifacts; the Lean tree had no definition to attach it to | **the conjecture is now a precise in-tree statement with its negative half proven and sample positives kernel-checked; the general positive half is THE named open item (sheared-CA sketch recorded in the chain doc)** |

## What did **not** change (deliberately)

- **SEE (Scalar Edge-Center Exhaustion), MAR, the L1–L7 collar clauses, the uniform product-thickening gate** — still named physical hypotheses. Formalizing them as *hypotheses* is the point; discharging them is physics, not Lean. (v4 made the *consequences* of the collar clauses theorem-form; the clauses themselves are untouched.)
- **G9 (record-ΔS → gravity-ΔS numerical bridge)** — still open. v4 built the record-side formal object (`DeltaSBridge.lean`), which sharpens what G9 must supply: the numerical calibration of that generator against gravity-side ΔS. A null still bounds only `χ·ΔS`.
- **L2.11 (force law)** — the *derivation* from the lab anomaly law is now one machine-checked FTC step (`DarkSector.thin_device_force`); the response law itself remains the declared input. `EnergyCage.lean` still prices it.
- **P's source branch** — unchanged: `ProotReported` enters as the executed solver's published numeral; the two-branch structure and χ-consequences are what is machine-checked.
- ~~The three `sorry`s in `observer-patch-holography/LEAN/Primitives.lean` are untouched~~ — **superseded in v4**: this tree now carries an attributed copy of the core with all three discharged (`Core/Primitives.lean`). The OPH team's own repo is still untouched (their call); upstreaming from here is a file move.

## Related audits

`../formal_audits/` contains raw audits of the `dula/` repos (Aristotle-generated Lean corpora). Verdict in short: not proof-chain-relevant (see `PIE_AUDIT_RAW.md`, `DULA_REPOS_AUDIT_RAW.md`, and proof chain v3 §9).

## File-level axiom audit

Every module ends with `#print axioms` on its headline theorems. Full sweep
after the v8 campaign (2026-07-07): **environment-level `collectAxioms` over
all 1199 non-internal theorem/def declarations in BOTH namespaces
(`OPH.*` + `OPHProofChain.*`), across 33 modules** (fresh `lake build`,
8282 jobs, clean — the same stronger-than-self-audit method the adversarial
audit used; count filter: `.thmInfo`/`.defnInfo` constants, `isInternal`
names excluded — earlier campaign counts used a wider constant filter, so
counts across versions are not directly comparable; the invariant that
matters is CLEAN);
all report subsets of `[propext, Classical.choice, Quot.sound]`;
`symmetricPair_not_locallyConfluent`, `symmetricPair_normalForm_iff` (fully
axiom-free), and `quorum_overlap_gap` report strictly fewer than the trio. **No `sorryAx` anywhere in the project —
including `Core/Primitives.lean`, whose three source `sorry`s are
discharged here.** (`lake build` output greps: `sorryAx` count 0; no axiom
line outside the standard trio.)

The only remaining build warnings are cosmetic: four "automatically included
section variable(s) unused" notes in the untouched generic section of the
imported `Core/Primitives.lean`, and a handful of pre-existing v3 linter
notes (`push_neg` deprecation etc.) kept to preserve the v3 files byte-for-byte.
